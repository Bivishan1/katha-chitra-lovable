Run this whenever you change the database schema :
npx supabase gen types typescript \ --linked \ > src/integrations/supabase/types.ts


Generated route tree (esp. in tanstack router):
src/routeTree.gen.ts
npx tsr generate

create new migration if you need to modify in sql, and then run following to update and push the migration:
`npx supabase db push`

`npx supabase status` = 

`npx supabase migration list`  = returns all the migrations (sql files pushed to the db) list

`npx supabase projects list` = project details

kathaChitra_$admin#
taskkill /PID 12345 /F


types.ts, routetree.gen.ts

-- to kill the used ports
Windows (PowerShell / CMD)Check: Find the Process ID (PID) using the port (replace 3000 with your port):cmdnetstat -ano | findstr :5173
Use code with caution.Delete: Kill the process using the PID found at the far right of the netstat output (e.g., 12345):cmdtaskkill /PID 12345 /F

-- to run sql file in local vscode:
1. npx supabase start
2. npx supabase db query --file supabase/seed-frames.sql (run this fter a while)

// Stop tracking .env file
1. first remove from tracking : git rm --cached .env
2. open .gitignore, type .env (or .env.local, whatever you have) on a new line. Save the file.
3. Run `git add .gitignore` and git commit -m "Stop tracking .env file" to save the changes.
